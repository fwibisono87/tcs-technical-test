version: "3.8"

services:
  stackunderflow:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: stackunderflow-app
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
